{
  "name": "jodoh",
  "version": "0.1.0",
  "description": "Jodoh — a paid CROO CAP agent that matches an agent's need to the best-fit provider on the Agent Store (reputation-weighted) and can hire it on the buyer's behalf for a rake.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "tsx src/agent.ts",
    "dev": "tsx watch src/agent.ts",
    "match": "tsx src/cli.ts",
    "demo": "tsx src/demo.ts",
    "buyer": "tsx examples/requester.ts",
    "health": "tsx examples/healthcheck.ts",
    "test": "tsx test/match.test.ts && tsx test/report.test.ts && tsx test/routing.test.ts"
  },
  "dependencies": {
    "@croo-network/sdk": "^0.2.1",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.7.0"
  }
}
